APP_NAME="Vel-SCADA"
APP_ENV=local
APP_KEY=base64:2jA1KKTp3L97DhMrovcyl4hqh4qLaMRR9fPt6KA38/s=
APP_DEBUG=true
APP_TIMEZONE=Asia/Jakarta
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database - MySQL (Docker)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=vel_scada
DB_USERNAME=root
DB_PASSWORD=password

# Session
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting - Reverb (WebSocket)
BROADCAST_CONNECTION=reverb

# Filesystem
FILESYSTEM_DISK=local

# Queue - Redis
QUEUE_CONNECTION=redis

# Cache - Redis
CACHE_STORE=redis
CACHE_PREFIX=vel_scada_

# Redis
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@vel-scada.local"
MAIL_FROM_NAME="${APP_NAME}"

# Reverb (WebSocket)
REVERB_APP_ID=vel-scada
REVERB_APP_KEY=vel-scada-key
REVERB_APP_SECRET=vel-scada-secret
REVERB_HOST=reverb
REVERB_PORT=8080
REVERB_SCHEME=http

# Vite
VITE_APP_NAME="${APP_NAME}"
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="localhost"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# ML Service
ML_SERVICE_URL=http://ml-service:8001
ML_SERVICE_TIMEOUT=30
